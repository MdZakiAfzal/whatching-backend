services:
  whatching-backend:
    build: .
    container_name: watching-backend
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules